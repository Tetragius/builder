import styled from 'styled-components';

export const Box = styled.iframe`
    height: calc(100%);
    width: 100%;
    border: none;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    display: block;
`;